meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: true
run:
  image: aquapau/negotiation-web:latest
  command: null
  args: null
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"